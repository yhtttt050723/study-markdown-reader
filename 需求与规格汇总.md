# Drillly 做题本 — 需求与规格汇总（Study 工作台版）

| 项目 | 说明 |
|------|------|
| 项目名称 | Drillly（做题本）· **Study 工作台子应用** |
| 文档版本 | v1.1（Study 集成） |
| 技术栈 | **React 19 + Vite 8 + TypeScript** · **FastAPI + SQLAlchemy** · **SQLite（本地）** / PostgreSQL（可选 Docker） |
| 仓库位置 | `d:\Study\drillly\`（`web/` + `api/`）；与 `md-reader-app`、`video-dash` 并列 |
| 一键启动 | 仓库根目录 **`启动Study工作台.ps1`**（端口见 **`study-suite.ports.json`**） |
| 生成说明 | 功能需求与 v1.0 一致；技术栈与联动方式按 Study 生态调整 |
| 更新日期 | 2026-05-18 |

---

## 目录

0. [Study 工作台集成](#0-study-工作台集成)
1. [项目概述](#1-项目概述)
2. [功能需求（主需求 P0）](#2-功能需求主需求-p0)
3. [功能需求（次需求 P1）](#3-功能需求次需求-p1)
4. [标签与分类](#4-标签与分类)
5. [草稿画板](#5-草稿画板)
6. [代码运行与提交记录](#6-代码运行与提交记录)
7. [Markdown 导出](#7-markdown-导出)
8. [非功能需求](#8-非功能需求)
9. [系统架构](#9-系统架构)
10. [数据模型](#10-数据模型)
11. [API 端点清单](#11-api-端点清单)
12. [UI 设计规范摘要](#12-ui-设计规范摘要)
13. [本地开发与部署](#13-本地开发与部署)
14. [里程碑与实现状态](#14-里程碑与实现状态)
15. [开放问题](#15-开放问题)
16. [术语表](#16-术语表)

---

## 0. Study 工作台集成

### 0.1 为何调整技术栈

原方案 **Django + DRF + PostgreSQL** 适合团队部署，但对个人 **`d:\Study`** 本地开发偏重：迁移慢、异步 PDF/LLM 需额外 Celery、与现有 **Vite 前端** 技术栈不统一。

**现方案（本地优先）**

| 层级 | 选型 | 说明 |
|------|------|------|
| 前端 | React 19、Vite 8、TypeScript、KaTeX、Monaco | 与 `md-reader-app`、`video-dash` 一致 |
| 后端 | **FastAPI**、Pydantic v2、SQLAlchemy 2 | 异步友好（LLM/上传）；自动 OpenAPI |
| 数据库 | **SQLite** 单文件 `drillly/data/drillly.db` | 零配置；生产可换 PostgreSQL（`docker compose` 可选） |
| 后台任务 | `asyncio` 后台任务（M2+ 可接 Redis/Celery） | M0/M0+ 按批同步解析即可 |
| 代码运行 | 本机 `python` / `javac` / `gcc` / `g++` | 逻辑不变；`MINGW_BIN` 仍适用 |

**功能需求（F-xxx）不变**；实现目录由 `backend/` + `frontend/` 调整为推荐 **`drillly/api/`** + **`drillly/web/`**（兼容旧名 `backend`/`frontend`）。

### 0.2 多应用联动

| 应用 | 目录 | 开发端口 | 与 Drillly 的关系 |
|------|------|----------|-------------------|
| Study Markdown Reader | `md-reader-app/` | **5210** | 主阅读器；打开 `学习资料/`、`周期记录/`；错题/二刷/视频进度看板 |
| video-dash | `video-dash/` | **5211** | 写回 `学习资料/学习视频进度/*.md` |
| **Drillly** | `drillly/web` + `drillly/api` | **5212** / **5213** | 题库、练习、PDF 导入、代码运行 |
| kb-server（可选） | `md-reader-app/kb-server/` | **5214** | 笔记向量库（`-WithKb`） |

**数据总线（推荐）**：不强制共享数据库，以 **Markdown + 约定路径** 联动：

| 路径 | 写入方 | 读取方 |
|------|--------|--------|
| `学习资料/学习视频进度/` | video-dash | Reader「视频进度」 |
| `学习资料/408/`、`数学/`、`英语/` 等 | 人工 / MDC | Reader 错题、二刷 |
| `学习资料/做题/`（规划） | Drillly 导出 | Reader / 人工归档 |
| `周期记录/学习进度.md`（`smr-progress`） | Reader | Reader「学习进度」 |

Drillly **Markdown/ZIP 导出**（F-030—034）默认写入 `学习资料/做题/export/`，便于与 Reader 同盘扫描。

### 0.3 统一启动

```powershell
cd D:\Study
.\启动Study工作台.ps1          # Reader + video-dash + Drillly（若已建仓）
.\启动Study工作台.ps1 -ReaderOnly
.\启动Study工作台.ps1 -WithKb   # 另启知识库 API
```

总览见 [`Study工作台.md`](Study工作台.md)。

---

## 1. 项目概述

Drillly 是一个在线做题与题库管理平台。

### 1.1 目标用户

- 备考/刷题的个人用户
- 需要维护结构化题库的教研人员

### 1.2 核心价值

- **自动化录入**：PDF → 大模型 → 结构化题目（LaTeX JSON），减少手工排版
- **统一渲染**：LaTeX + KaTeX 保证公式显示一致
- **分类与标签**：按分类、标签组织与筛选题目
- **完整作答轨迹**：选择题作答与代码题历次提交均可追溯
- **可导出**：Markdown / ZIP 便于备份、打印、迁移

### 1.3 技术栈

| 层级 | 技术 |
|------|------|
| 前端 | React 19、Vite 8、TypeScript、KaTeX、Monaco Editor |
| 后端 | FastAPI、SQLAlchemy 2、Alembic（迁移） |
| 数据库 | SQLite（本地默认 `drillly/data/drillly.db`）；PostgreSQL（可选生产） |
| 模型 | 通义千问（DashScope）、DeepSeek（OpenAI 兼容）；Key 仅后端 |
| 部署形态 | Study 工作台子应用；与 Reader / video-dash **端口分离、数据 Markdown 联动** |

---

## 2. 功能需求（主需求 P0）

### 2.1 PDF 导入与模型解析

| 编号 | 需求 | 验收标准 | 实现状态 |
|------|------|----------|----------|
| F-001 | HTTP API 接收题目 PDF | `multipart/form-data`，仅 PDF，大小可配置（默认 ≤20MB） | ✅ |
| F-002 | 调用 LLM 解析题目 | API Key 仅后端；支持通义、DeepSeek | ✅ |
| F-003 | 返回结构化 JSON | JSON Schema 校验；含题干、选项、答案、解析 | ✅ |
| F-004 | 数学内容为 LaTeX 字符串 | 存 PostgreSQL/SQLite `JSONB`；前端 KaTeX 渲染，禁止危险 HTML | ✅ |
| F-005 | 解析任务可异步 | 上传返回 `task_id`；可按批次查询状态 | ✅（按批同步；M2+ FastAPI BackgroundTasks / 队列） |
| F-006 | 预览后确认入库 | 解析 → 预览 JSON →「入库」写入题目表 | ✅ |
| F-007 | **PDF 按页码拆分** | `drillly/api/tools/split_pdf.py`（或 `backend/tools/`）；`pages_per_batch` 默认 5（1–20） | ✅ |
| F-008 | 每批独立子任务 | 记录起止页码，如「第 1–5 页」 | ✅ |
| F-009 | 拆分片段存储 | `media/imports/chunks/` | ✅ |
| F-010 | **导入时绑定 PDF 标签** | 上传表单 `tags` 字段；入库题目继承 PDF 标签 | ✅ |

**PDF 拆分流程：**

```
上传 PDF（可带 tags）
  → 计算总页数 → split_pdf(每批 N 页)
  → 创建 PdfImportTask + PdfImportBatch[]
  → 逐批调用 LLM（tongyi / deepseek / mock）
  → 预览 JSON → 确认入库 → Question 记录
```

**题目 JSON 结构（`drillly/api/schemas/question.json`）：**

```json
{
  "type": "single_choice | multiple_choice | coding",
  "title": "题目标题",
  "stem": "题干，LaTeX：已知 $f(x)=x^2$",
  "options": [{ "key": "A", "content": "选项内容" }],
  "answer": ["A"],
  "explanation": "解析",
  "language": "python|java|c|cpp",
  "metadata": {
    "difficulty": "easy | medium | hard",
    "tags": ["题目标签1", "题目标签2"],
    "source_pdf": "文件名",
    "page": 1
  }
}
```

---

### 2.2 前端公式渲染（KaTeX）

| 编号 | 需求 | 验收标准 | 实现状态 |
|------|------|----------|----------|
| F-011 | LaTeX 渲染 | `react-katex`；行内 `$...$`、块级 `$$...$$` | ✅ |
| F-012 | 支持 Markdown | 可与 KaTeX 共存 | 部分 |
| F-013 | 渲染失败降级 | 显示原始 LaTeX，不白屏 | ✅ |

---

### 2.3 做题功能

| 编号 | 需求 | 验收标准 | 实现状态 |
|------|------|----------|----------|
| F-020 | **单选题** | 互斥选项；提交判分 | ✅ |
| F-021 | **多选题** | 多选；默认全对才得分 | ✅ |
| F-022 | **代码题** | Monaco；Python / Java / C / C++ | ✅ |
| F-023 | 记录每次作答 | 答案、得分、耗时、时间戳 | ✅ |
| F-024 | 练习筛选 | 按分类、标签筛选题目 | ✅ |
| F-025 | 交卷后展示解析 | KaTeX 渲染 `explanation` | ✅ |
| F-026 | **右侧草稿画板** | Canvas；按题 `localStorage` 保存 | ✅ |
| F-027 | **代码提交记录** | 列表展示历次代码；可展开、恢复代码 | ✅ |
| F-028 | 题目分类管理 | 每题一个分类；可筛选、可修改 | ✅ |

---

### 2.4 模型对接（通义 / DeepSeek）

| 编号 | 需求 | 说明 | 实现状态 |
|------|------|------|----------|
| F-040 | 通义千问 | `TONGYI_API_KEY`；默认 `qwen-plus` | ✅ |
| F-041 | DeepSeek | `DEEPSEEK_API_KEY`；默认 `deepseek-chat` | ✅ |
| F-042 | Mock 模式 | 无 Key 时可测流程 | ✅ |
| F-043 | 导入页选择模型 | 下拉选择提供商与模型 ID | ✅ |

环境变量见 `drillly/api/.env.example`（或 `backend/.env.example`）。

---

## 3. 功能需求（次需求 P1）

### 3.1 用户与权限

| 编号 | 需求 | 说明 | 实现状态 |
|------|------|------|----------|
| F-100 | 注册/登录 | JWT 或 Session | ⏳ 未做 |
| F-101 | 角色（用户/管理员） | 审核 PDF、管理题库 | ⏳ 未做 |
| F-102 | 题库私有/公开 | 默认私有 | ⏳ 未做 |

### 3.2 题库管理

| 编号 | 需求 | 说明 | 实现状态 |
|------|------|------|----------|
| F-110 | 题库 CRUD、难度 | QuestionBank 模型已有 | 部分 |
| F-111 | 全文/标签搜索 | PostgreSQL tsvector | ⏳ 未做 |
| F-112 | 随机出题、未做题优先 | 练习模式增强 | ⏳ 未做 |

### 3.3 网页部署

| 编号 | 需求 | 说明 | 实现状态 |
|------|------|------|----------|
| F-120 | 前后端分离部署 | Docker Compose 提供 PostgreSQL | 部分 |
| F-121 | 环境变量管理 | `.env`，密钥不进仓库 | ✅ |
| F-122 | HTTPS、HSTS | 反向代理终止 TLS | ⏳ 生产未配 |

---

## 4. 标签与分类

### 4.1 概念区分

| 概念 | 说明 | Cardinality |
|------|------|-------------|
| **分类 (Category)** | 固定大类：数学、编程、考研… | 每题 **0 或 1** 个 |
| **标签 (Tag)** | 细粒度：代数、Python、2024 真题… | 每题 **多个** |
| **PDF 标签** | 导入 PDF 时设置，入库时写入题目 | 导入任务级 |

### 4.2 功能要求

| 编号 | 需求 | 验收标准 | 实现状态 |
|------|------|----------|----------|
| F-050 | 标签 CRUD | `GET/POST /api/questions/tags/` | ✅ |
| F-051 | 题目绑定标签 | 导入 PDF 时、metadata.tags、手动 | ✅ |
| F-052 | 按标签筛选练习 | 练习页多选标签过滤 | ✅ |
| F-060 | 分类 CRUD | `GET/POST /api/questions/categories/` | ✅ |
| F-061 | 题目设置分类 | 题目卡片下拉；可新建分类 | ✅ |
| F-062 | 按分类筛选练习 | 练习页分类按钮组 | ✅ |
| F-063 | 导出按分类筛选 | `?category=` 参数 | ✅ |

---

## 5. 草稿画板

| 编号 | 需求 | 验收标准 | 实现状态 |
|------|------|----------|----------|
| F-070 | 右侧固定 360px Canvas | 与主区左右分栏，见 DESIGN.md | ✅ |
| F-071 | 画笔 / 橡皮 / 颜色 / 线宽 | 底部工具栏 | ✅ |
| F-072 | 按题保存 | `localStorage`：`drillly-scratch-{questionId}` | ✅ |
| F-073 | 清空确认 | 二次确认；不恢复已删 localStorage | ✅ |
| F-074 | **快捷键** | Ctrl+Z 撤销、Ctrl+Y/Ctrl+Shift+Z 重做、Ctrl+C/V 复制粘贴 | ✅ |
| F-075 | 画板聚焦提示 | 聚焦时边框高亮 | ✅ |

---

## 6. 代码运行与提交记录

### 6.1 本地代码运行

| 编号 | 需求 | 验收标准 | 实现状态 |
|------|------|----------|----------|
| F-080 | 四语言运行 | `POST /api/runner/execute/` | ✅ |
| F-081 | 超时与输出限制 | 默认 10s；输出 64KB 截断 | ✅ |
| F-082 | Windows 找编译器 | 自动搜 MSYS2/MinGW；`MINGW_BIN` 环境变量 | ✅ |
| F-083 | 友好错误提示 | 未安装 gcc/g++ 时中文说明 | ✅ |

**环境变量：**

```env
MINGW_BIN=C:\msys64\ucrt64\bin
GCC_PATH=
GPP_PATH=
RUNNER_TIMEOUT_SEC=10
```

### 6.2 代码提交记录

| 编号 | 需求 | 验收标准 | 实现状态 |
|------|------|----------|----------|
| F-090 | 每次提交持久化 | `Submission` 表；代码全文 + 语言 + 时间 | ✅ |
| F-091 | 按题查询记录 | `GET /api/practice/submissions/?question_id=` | ✅ |
| F-092 | 前端展示列表 | 题目下方「提交记录」面板 | ✅ |
| F-093 | 展开查看完整代码 | 深色代码块展示 | ✅ |
| F-094 | 恢复历史代码 | 「恢复此代码」载入编辑器 | ✅ |
| F-095 | 选择题也记录 | 选项/得分/时间 | ✅ |

**提交时存储结构（代码题）：**

```json
{
  "value": "源代码全文",
  "code": "源代码全文",
  "language": "python"
}
```

---

## 7. Markdown 导出

| 编号 | 需求 | 验收标准 | 实现状态 |
|------|------|----------|----------|
| F-030 | 导出为 Markdown | 含题干、选项、分类、标签 | ✅ |
| F-031 | 批量导出 ZIP | 每题一文件 + 合集 + 作答汇总 | ✅ |
| F-032 | 导出作答记录 | 含代码块、正误、时间 | ✅ |
| F-033 | 可选含答案 | `include_answers=true` | ✅ |
| F-034 | 按标签/分类筛选导出 | `tags`、`category` 查询参数 | ✅ |

**API：** `GET /api/practice/export/markdown/`

| 参数 | 说明 |
|------|------|
| `tags` | 逗号分隔标签名 |
| `category` | 分类 ID 或名称 |
| `include_answers` | `true` / `false` |
| `include_submissions` | `true` / `false`（默认 true） |
| `format` | `single`（单 MD）/ `zip` |

---

## 8. 非功能需求

### 8.1 安全性（生产必达）

| 编号 | 要求 | 实现状态 |
|------|------|----------|
| SEC-001 | 全站 HTTPS；Secure Cookie | ⏳ |
| SEC-002 | API 鉴权 | ⏳ |
| SEC-003 | PDF 上传 MIME 校验、随机文件名 | ✅ |
| SEC-004 | 模型 API Key 仅后端 | ✅ |
| SEC-005 | ORM + JSON Schema 防注入 | ✅ |
| SEC-006 | XSS：React 转义；禁止任意 HTML | ✅ |
| SEC-007 | 代码沙箱（Docker/gVisor） | ⏳ 本地直连本机 |
| SEC-008 | 限流 | ⏳ |
| SEC-009 | CORS 白名单 | ✅（`5210–5212` Study 套件端口，见 `study-suite.ports.json`） |
| SEC-010 | 防暴力破解 | ⏳ |
| SEC-011 | 审计日志 | ⏳ |

### 8.2 性能与可用性

| 编号 | 要求 |
|------|------|
| NFR-001 | 题目列表分页，单页 ≤ 50 条 |
| NFR-002 | PDF 解析不阻塞主线程（推荐 Celery） |
| NFR-003 | 数据库连接池；静态资源缓存 |
| NFR-004 | 可用性：维护窗口 < 4h/月 |

### 8.3 可维护性

- 目录：`drillly/api/`、`drillly/web/`、`docs/`、仓库根 `Study工作台.md`
- 题目 Schema：`drillly/api/schemas/question.json`
- 核心逻辑：`api/routers/`（practice、import_pdf、runner、questions）

---

## 9. 系统架构

```
                    d:\Study\学习资料\  (*.md 数据总线)
                              ▲
          ┌───────────────────┼───────────────────┐
          │                   │                   │
┌─────────┴────────┐  ┌───────┴────────┐  ┌──────┴───────┐
│ md-reader-app    │  │ video-dash     │  │ Drillly Web  │
│ :5210 Electron   │  │ :5211          │  │ :5212        │
│ 进度/错题/视频板  │  │ 写 BV 分 P md  │  │ KaTeX/Monaco │
└─────────┬────────┘  └────────────────┘  └──────┬───────┘
          │                                      │ HTTP
          │                              ┌───────▼────────┐
          │                              │ FastAPI :5213  │
          │                              │ SQLite + 导出  │
          │                              └───────┬────────┘
          │                    ┌────────────────┼────────────────┐
          │                    ▼                ▼                ▼
          │              ┌──────────┐    ┌──────────┐    ┌──────────┐
          │              │ 通义 API │    │ DeepSeek │    │ 本机 gcc │
          └──────────────┴──────────┴────┴──────────┴────┴──────────┘
```

**目录结构（Study 仓库内）：**

```
d:\Study\
├── 启动Study工作台.ps1
├── study-suite.ports.json
├── Study工作台.md
├── 需求与规格汇总.md          # 本文档
├── md-reader-app/             # 主入口
├── video-dash/
├── drillly/                   # 做题本（实现中）
│   ├── api/                   # FastAPI
│   │   ├── routers/
│   │   ├── tools/split_pdf.py
│   │   └── schemas/question.json
│   └── web/                   # Vite SPA
├── 学习资料/
│   ├── 学习视频进度/          # video-dash ↔ Reader
│   └── 做题/                  # Drillly 导出（规划）
└── 周期记录/
```

---

## 10. 数据模型

| 实体 | 主要字段 | 说明 |
|------|----------|------|
| **Category** | id, name, color, sort_order, parent | 题目分类 |
| **Tag** | id, name, color | 题目标签 |
| **QuestionBank** | id, name | 题库 |
| **Question** | id, bank_id, category_id, type, content (JSONB), status, tags M2M | 题目 |
| **Submission** | id, question_id, answer (JSONB), score, is_correct, duration_ms, created_at | 作答/代码提交 |
| **PdfImportTask** | id, original_name, file_path, total_pages, pages_per_batch, status, tags M2M | PDF 导入任务 |
| **PdfImportBatch** | id, task_id, page_start, page_end, chunk_path, status, raw_response | 分批解析 |
| **User** | — | P1 待实现 |

---

## 11. API 端点清单

### 11.1 题目与元数据

| 方法 | 路径 | 说明 |
|------|------|------|
| GET/POST | `/api/questions/` | 题目列表/创建；`?tags=` `?category=` `?type=` |
| GET/PATCH/DELETE | `/api/questions/{id}/` | 题目详情/更新（含 category） |
| GET/POST | `/api/questions/tags/` | 标签列表/创建 |
| GET/POST | `/api/questions/categories/` | 分类列表/创建 |

### 11.2 练习与提交

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/api/practice/submit/` | 提交作答 `{ question_id, answer, language?, duration_ms }` |
| GET | `/api/practice/submissions/` | 提交记录 `?question_id=` |
| GET | `/api/practice/export/markdown/` | Markdown/ZIP 导出 |

### 11.3 PDF 导入

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/import/providers/` | 可用模型列表 |
| POST | `/api/import/pdf/` | 上传 PDF；`tags`、`pages_per_batch` |
| GET | `/api/import/tasks/{id}/` | 任务详情 + 批次 |
| GET | `/api/import/tasks/{id}/batches/{bid}/` | 批次解析结果 |
| POST | `/api/import/tasks/{id}/batches/{bid}/parse/` | 调用模型 `{ provider, model }` |
| POST | `/api/import/tasks/{id}/batches/{bid}/confirm/` | 解析结果入库 |

### 11.4 代码运行

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/api/runner/execute/` | `{ language, code, stdin? }` |

### 11.5 鉴权（P1 规划）

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/api/auth/login/` | 登录（未实现） |

---

## 12. UI 设计规范摘要

> 完整版见 `docs/DESIGN.md`

### 12.1 设计原则

1. 做题优先：主区展示当前题目
2. 左右分栏：左侧作答，右侧 360px 草稿画板
3. 低干扰：中性色 + 蓝色主色
4. 可访问性：对比度、焦点环

### 12.2 核心页面

**练习页：** 顶栏 → 分类/标签筛选 + 导出区 → 题目卡片（分类、标签、题干、选项/编辑器、提交记录）→ 右侧画板

**导入页：** 解析设置（每批页数、模型）→ PDF 标签 → 上传区 → 批次表格（解析、预览、入库）

### 12.3 设计令牌（节选）

| Token | 值 |
|-------|-----|
| `--color-primary` | `#2563eb` |
| `--color-success` | `#059669` |
| `--color-error` | `#dc2626` |
| `--header-height` | `56px` |
| `--scratch-width` | `360px` |

### 12.4 题型 Badge

| 类型 | 样式 |
|------|------|
| 单选 | 蓝底 `#dbeafe` |
| 多选 | 紫底 `#e0e7ff` |
| 代码 | 绿底 `#d1fae5` |

---

## 13. 本地开发与部署

### 13.1 环境要求

- Python 3.11+
- Node.js 20+（与 `md-reader-app` / `video-dash` 一致）
- 代码运行：`python`、`javac`/`java`、`gcc`、`g++`（Windows 建议 MSYS2）

### 13.2 启动命令（推荐：Study 工作台一键）

```powershell
cd D:\Study
.\启动Study.ps1
# 或双击 启动Study.bat
```

| 服务 | URL |
|------|-----|
| Markdown Reader | http://localhost:5210 |
| video-dash | http://localhost:5211 |
| Drillly 练习 / 导入 | http://localhost:5212 · http://localhost:5212/import |
| Drillly API | http://127.0.0.1:5213/docs |

**仅 Drillly 手动启动（调试）：**

```powershell
cd drillly\api
.\run.bat    # 默认端口 5213

cd drillly\web
npm run dev  # 默认 5212，代理 /api → 5213
```

### 13.3 环境变量（`drillly/api/.env`）

```env
TONGYI_API_KEY=
DEEPSEEK_API_KEY=
LLM_DEFAULT_PROVIDER=tongyi
MINGW_BIN=C:\msys64\ucrt64\bin
PDF_DEFAULT_PAGES_PER_BATCH=5
```

### 13.4 初始化数据

```powershell
cd drillly\api
.\.venv\Scripts\alembic upgrade head
.\.venv\Scripts\python -m scripts.seed_demo
```

（若仍使用 Django 旧树：`manage.py migrate` + `seed_demo`。）

### 13.5 PDF 拆分 CLI

```powershell
python drillly\api\tools\split_pdf.py 试卷.pdf -o .\chunks -b 5
```

---

## 14. 里程碑与实现状态

| 阶段 | 交付物 | 状态 |
|------|--------|------|
| **M0** | 本地可运行：练习、画板、四语言运行、示例题 | ✅ 已完成 |
| **M0+** | PDF 拆分、通义/DeepSeek、标签、分类、提交记录、Markdown 导出 | ✅ 已完成 |
| M1 | 题目 CRUD 完善、OpenAPI 文档 | 进行中 |
| M2 | PDF 全流程 + 异步 Worker | 部分 |
| M3 | 鉴权、多用户 | ⏳ |
| M4 | 生产部署 HTTPS、沙箱判题 | ⏳ |
| M5 | 安全加固、限流、审计 | ⏳ |

**图例：** ✅ 已实现 · ⏳ 未实现 · 部分 部分实现

---

## 15. 开放问题

1. 是否引入 Celery 做 PDF/LLM 真异步？
2. 代码题生产环境沙箱方案（Docker / 远程判题）？
3. 多用户与题库权限模型？
4. 扫描版 PDF 是否接入 OCR / 视觉模型？
5. 移动端与离线支持？
6. 暗色主题（DESIGN.md P2）？

---

## 16. 术语表

| 术语 | 定义 |
|------|------|
| 题干 (stem) | 题目正文，可含 LaTeX |
| LaTeX JSON | JSON 存储题目结构，数学为 LaTeX 字符串 |
| 分类 (Category) | 题目所属大类，单选 |
| 标签 (Tag) | 题目标签，可多选 |
| PDF 标签 | 导入 PDF 时设置，入库题目继承 |
| 导入批次 | 按页码拆分后的 PDF 片段及模型请求单元 |
| 草稿画板 | 右侧 Canvas，演算草图，`localStorage` 持久化 |
| 提交记录 | Submission，含代码题历次代码快照 |

---

## 附录：相关文档索引

| 文件 | 用途 |
|------|------|
| `需求与规格汇总.md` | **本文档**（仓库根，Study 版） |
| `Study工作台.md` | 多应用联动与一键启动 |
| `study-suite.ports.json` | 各服务端口 |
| `启动Study.ps1` / `启动Study.bat` | 统一启动脚本（开服务 + 浏览器） |
| `md-reader-app/README.md` | Reader 功能说明 |
| `video-dash/README.md` | B 站进度与文件夹同步 |
| `drillly/api/.env.example` | Drillly 环境变量（待建） |
| `drillly/docs/DESIGN.md` | UI 设计规范（待建） |

---

*本文档随功能迭代更新。技术栈以 **FastAPI + SQLite + Vite** 为准；**F-xxx 功能验收不变**。*
