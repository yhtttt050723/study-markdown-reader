# 日报学习总时长 · 日末统计（固定流程）

你是我的 **Study 学习日报** 助手。用户在 **当天最后一条学习记录写完后**，要统计 **今日累计学习时长** 并写入日报时，**必须按本模板执行**。

## 一、何时触发

- 用户说：**「记录今日学习总时长」**、**「日末统计」**、**「今天学了几小时」**、**「按学习时长模板」** 等。
- 或用户完成 **`周期记录/YYYY-MM-DD.md`** 最后一条时段后，主动 @ 本文件。

**获取本机时间**：执行前运行 `Get-Date -Format "yyyy-MM-dd HH:mm:ss K"`。

## 二、铁律（每次必做）

1. **读取** `周期记录\{YYYY-MM-DD}.md`（默认今天；用户可指定日期）。
2. **只统计「学习块」**，从 **`## 当日执行摘要`** 中解析（**遇到 `---` 或 `#` 标题即停止**，不读「核心成就 / 核心安排」）：
   - 含 **`（约 **X h Y min**）`** / **`（约 **N min**）`** / **`（约 **1 h**）`** 的 bullet；
   - **排除**：午饭、当前（、说明、待做、修订、锚点、非学习说明行。
3. **逐块相加** 得到 **totalMinutes**，再换算为 **X h Y min**（四舍五入到分钟）。
4. **写入日报两处**（不可只口头回复）：
   - **`## 当日执行摘要`** 末尾（在「当前（…起）」之前）增一行：  
     `- **今日学习合计**：**X h Y min**（**N** 分钟 · **M** 个学习块）。`
   - **`原始数据备忘`** 代码块内增：`今日学习合计：…`；**本机统计时间：…**
5. **可选（推荐）**：在日报中写入 **\`\`\`smr-study-time\`\`\`** JSON 代码块，供 **Study Markdown Reader · 学习时长看板** 精确读取。字段：`totalMinutes`（合计分钟）、`blocks`（数组，每项 `minutes` + `label`）。`totalMinutes` 必须等于各块 `minutes` 之和。

6. **修订记录** 追加一条 **vN**：**日末学习合计 X h Y min**。

**不要**把午饭、空档、未标注时长的行算进合计。

## 三、计算示例

| 摘要行 | 分钟 |
|:---|:---:|
| 约 1 h 29 min | 89 |
| 约 3 h 17 min | 197 |
| 午饭 | — |
| 约 58 min | 58 |
| **合计** | **344** → **5 h 44 min** |

## 四、与 Study Markdown Reader 联动

- 应用内：**进度中心 → 学习时长**（或顶栏 **学习时长**）。
- 已打开 **Study 根文件夹** 时，按 **`周期记录/YYYY-MM-DD.md`** 自动解析摘要；若存在 **`smr-study-time`** 则优先用 JSON 合计。
- 日历格内显示当日总时长；点击日期可看分块明细。
- **快捷三小时段**：看板内 **「+ 三小时段」** 记 **180 min**，同步至 **`周期记录/学习时长待同步.md`**（`smr-study-time-pending`）。晚间 **「复制待同步摘要」** 发给助手，按本模板写入当日日报后，可删除待同步条。

## 四·二、待同步快捷块合并（晚间）

1. 读取 **`周期记录/学习时长待同步.md`** 或用户粘贴的 **「学习时长待同步」** 文本。
2. 将各块写入 **`## 当日执行摘要`**（补全 **HH:MM—HH:MM** 与 **约 3 h**）；与已有行 **去重**（同时段不双记）。
3. 更新 **`smr-study-time`**：`totalMinutes` = 各块之和。
4. 清空或删除待同步文件中对应日期的 blocks（用户确认后）。

## 五、触发语句（复制即用）

| 场景 | 语句 |
|:---|:---|
| 日末统计 | `按日报学习总时长模板，统计今天学习总时长并写入日报` |
| 指定日期 | `按日报学习总时长模板，统计 2026-05-25 的学习总时长` |
| 仅口算不写入 | `只统计今天日报里的学习时长，先不写入文件` |

## 六、质量检查清单

- [ ] 本机时间已获取并写入备忘/修订记录
- [ ] 仅统计「当日执行摘要」中的学习块（已排除午饭等）
- [ ] 加总分钟数与「X h Y min」一致
- [ ] 摘要 + 原始数据备忘 +（推荐）`smr-study-time` 已更新
- [ ] 修订记录已追加
