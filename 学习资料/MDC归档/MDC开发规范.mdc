# MDC 开发规范（写新模板前必读）

> 借鉴 [django-skills](https://github.com/saaspegasus/django-skills) 的 Agent Skill 写法，适配 Study 仓库 **`学习资料/MDC归档/*.mdc`** 执行模板。  
> **写完新 MDC 后**：在 **`提示词使用总览.md`** 登记触发语句。

---

## 一、django-skills 值得拿过来的点

| 做法 | django-skills | Study MDC 对应 |
|:---|:---|:---|
| **元数据** | `name` + `description`（第三人称，含 WHAT + WHEN） | 文首一句话 + **「一、何时触发」** 写清触发词 |
| **任务入口** | `## Your task` 一句话定目标 | 文首 **角色 + 必须完成的动作**（如「一次性落盘三处」） |
| **分步流程** | Step 1→N，顺序固定，失败即停 | **铁律** + 编号步骤；有 CLI 的写 **优先 CLI** |
| **可执行命令** | `allowed-tools` + 内嵌精确 shell | 贴 **完整 PowerShell**（含 `cd`、参数示例），禁止「去查一下」 |
| **决策分支** | 冲突时 **汇报并询问**，不擅自改 | **收到后直接执行，不要反问** vs **缺关键信息才问** 写死 |
| **事后校验** | post-upgrade checks → summarize | **写入后自检清单** + **修订记录 vN** |
| **渐进披露** | 主 SKILL 短，细节另文件 | 复杂模板拆 **附录** 或链到现有 MDC，正文 ≤ ~150 行 |
| **触发登记** | README 表格：Skill / Description / Triggers | **`提示词使用总览.md`** 必更新 |

**不必照搬**：django 的 `allowed-tools` 是 Claude Code 专用；Cursor 侧用 **明确命令块 + 铁律** 即可。

---

## 二、Study MDC 标准结构（复制骨架）

```markdown
# {任务名}（固定流程）

你是 **{角色}**。当用户 {场景} 时，**必须** {核心动作，一句话}。

## 一、何时触发

- 用户说：**「…」**、**「…」**
- 用户 @ 本文件，或说：**「按{模板名}…」**
- （可选）与 **`执行模板/xxx.mdc`** 联动说明

**获取本机时间**：执行 `Get-Date -Format "yyyy-MM-dd HH:mm:ss K"`（不得以会话静态日期代替）。

## 二、铁律（每次必做，不可省略）

1. …
2. …

## 三、用户输入模板（复制填写）

\`\`\`text
字段1：
字段2：
\`\`\`

收到后 **直接执行** / **缺 X 才反问**（二选一，写死）。

## 四、执行步骤

### Step 1：…

\`\`\`powershell
# 完整可粘贴命令
\`\`\`

### Step 2：写入文件

| 序号 | 文件 | 路径规则 |
|:---:|:---|:---|
| 1 | … | `相对路径` |

### Step 3：输出格式

（贴 markdown / JSON 代码块样例）

## 五、禁止事项

- 不要编造数据
- 不要只口头回复不落盘
- 不要 …

## 六、完成后自检

- [ ] 路径、日期、数量三处一致
- [ ] 已写修订记录 / smr-* JSON（若适用）
- [ ] 已在 `提示词使用总览.md` 登记触发语句
```

---

## 三、写 description / 触发语句（django 式）

**公式**（第三人称）：

> {做什么}。在用户 {说/做 X}、@ 本文件、或处理 {文件类型/目录} 时使用。

**好例子**（已上线）：

- `按做题本数据模板附挂` → 拉 Drillly SQLite，写入段内 Markdown + `smr-drillly-slot`
- `按英语默写错词模板录入：Unit N…` → 三文件落盘 + 二刷配额

**坏例子**：

- 「帮助整理学习」——太宽，Agent 不会稳定触发
- 「必要时查询数据库」——没有命令、没有路径

**触发语句规范**：

1. 一条主触发 + 1–2 条同义说法
2. 带参数的用占位：`按二刷导入模板生成 {YYYY-MM-DD} 的二刷任务`
3. 登记到 `提示词使用总览.md` 的 **执行模板 / 对应分类** 下

---

## 四、命令与工具（学 django 的「低自由度」）

凡涉及本仓库数据，**默认给可复制命令**：

| 场景 | 命令入口 |
|:---|:---|
| 本机日期 | `Get-Date -Format "yyyy-MM-dd HH:mm:ss K"` |
| Drillly 时段 | `drillly/api/scripts/query_session_stats.py` |
| 路径根 | `D:\Study\` 或仓库相对路径，**统一一种** |

规则：

- **优先 CLI**（API 未启动也能跑）> 再写 API 备选
- 参数用 **真实示例日期**，注明「替换为终端日期」
- 跨日时段写清 `--date` / `--end-date` 规则

---

## 五、输出与校验（学 post-upgrade checks）

### 5.1 必须落盘时

写清 **写几个文件、各写什么**；禁止「总结给用户看」就结束。

### 5.2 结构化块（可选）

日报类可带 `smr-*` JSON（如 `smr-drillly-slot`、`smr-study-time`），在 MDC 里给 **完整样例块**。

### 5.3 完成后自检清单（每条 MDC 末尾必有）

```markdown
## 完成后自检

- [ ] 本机时间已从终端读取
- [ ] …
- [ ] 用户可见处已说明「写入了哪些文件」
```

---

## 六、分类与命名

| 目录 | 用途 | 命名 |
|:---|:---|:---|
| `执行模板/` | 一次性任务、落盘流程 | `{动作}{对象}.mdc` |
| `激励周期/` | 打分、奖励、周期复盘 | 周期名.mdc |
| `计划与策略/` | 排计划、决策复盘 | 月份或主题.mdc |
| `科目目录/` | 章节索引（非执行流） | 科目.mdc |
| `学习视频进度/` | 视频看板同步 | 主题.mdc |

文件名：**中文可读**；同一动作不要拆成多个近似 MDC。

---

## 七、开发前 Checklist（开写前勾一遍）

```
MDC 开发前：
- [ ] 任务是否已有近似 MDC？有则 **扩写旧文件**，不新建重复
- [ ] 触发语句是否 ≤ 1 句能说清？
- [ ] 是否有「铁律」与「禁止编造」？
- [ ] 是否依赖本机时间？→ 已写 Get-Date 铁律
- [ ] 是否有 shell/SQLite/API？→ 已贴完整命令
- [ ] 输出是否有代码块样例（表格 / JSON / 段落）？
- [ ] 失败分支是否写明（无数据 / 跨日 / 文件已存在）？
- [ ] 正文是否可控制在 ~150 行（更长则拆附录）？
- [ ] 完成后是否会更新 `提示词使用总览.md`？
```

---

## 八、与 Cursor Skill 的分工

| 类型 | 位置 | 何时用 |
|:---|:---|:---|
| **MDC** | `学习资料/MDC归档/` | 学习流程、日报、错题、落盘格式；用户 **@ 或说触发句** |
| **Skill** | `.cursor/skills/` | 软件开发（Drillly 构建、夜间队列）；`/skill` 或描述触发 |
| **Rule** | `.cursor/rules/` | 全局铁律（如 `local-time-authority`） |

**不要**把日报落盘流程放进 Skill；**不要**把 npm build 写进 MDC。

新建 **软件开发** 流程 → 参考 django-skills + `.cursor/skills/nightly-study-update/SKILL.md`。  
新建 **学习执行** 流程 → 用本文件 + 现有 `执行模板/*.mdc` 仿写。

---

## 九、最小样例（django 式短模板）

```markdown
# 示例 · 单文件追加（固定流程）

你是 **复盘助手**。用户给出 **错因一句话** 时，必须追加到 `周期记录/{今日}.md` 的 `## 段 #N` 末尾。

## 一、何时触发

- 用户说：**「把这条写进段 #2」**

**获取本机时间**：`Get-Date -Format "yyyy-MM-dd"`

## 二、铁律

1. 先读目标文件，**追加**不覆盖
2. 追加 `> 复盘补充：…` 引用块
3. 修订记录 **vN**

## 三、禁止

- 不要新建别的日期的文件
- 不要编造段号

## 四、完成后自检

- [ ] 文件名日期 = 终端日期
- [ ] 已告知用户写入路径
```

---

*维护：新增或大幅改版任一 `执行模板/*.mdc` 时，同步检查本节是否需补充范例。*
