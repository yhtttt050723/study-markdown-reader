---
description: Study Markdown Reader（md-reader-app）代码规范与开发约定；修改本目录下软件相关代码前必读并遵守
globs: md-reader-app/**/*
alwaysApply: false
---

# md-reader-app 代码规范（必读）

本规则适用于 **`md-reader-app/`** 内 React 前端、样式与 Electron 主进程相关改动。开始改需求前，**先通读本文件**，再动手写代码。

## 技术栈与目录

- **渲染进程**：React 19 + Vite（`"type": "module"`），入口 **`src/main.jsx`**（内调用 **`initMarked(marked)`**），主界面 **`src/App.jsx`** + **`src/App.css`**。
- **Markdown**：`marked` 配置在 **`src/markedConfig.js`**；**错题/二刷解析、筛选、路径工具** 在 **`src/markdownQuiz.js`**；**`smr-*` 键与读数工具** 在 **`src/storageKeys.js`**。预览/刷题 HTML 使用 `dangerouslySetInnerHTML` 前须信任来源为本地文件内容。
- **Electron**：`electron/main.cjs`、`electron/preload.cjs` 为 **CommonJS**；与 `package.json` 的 ESM 并存，**不要随意把 main 改成 .mjs** 除非同步改 `package.json` 与打包配置。
- **构建产物**：`dist/`、`release/` 由脚本生成；**不要手改**；提交前以 `.gitignore` 为准。

## `App.jsx` 结构约定

1. **纯函数模块**：与 Markdown/题库相关的解析、筛选（`stripAnswerSectionsForPractice`、`parseWrongBlock`、`filterQuizPool`、`readMarkdownFileText`、`buildExpandedGroupsSeed` 等）放在 **`src/markdownQuiz.js`**，勿在 `App.jsx` 内重复大段正则；`App.jsx` 只负责状态、副作用与 UI 组合。
2. **Electron API**：凡调用 `window.electronAPI` 须先判断 **`hasApi` / `canNativeSave`** 或 `typeof xxx === "function"`**，避免浏览器模式或未注入时抛错。
3. **状态**：刷题相关状态（`quizFullPool`、`quizFilteredPool`、`quizFileOnlyMode` 等）与 UI 同文件维护时，**筛选逻辑**集中在 `filterQuizPool` + `useMemo`，避免多处复制过滤条件。
4. **题库条目形状**：错题本 `kind: "wrongbook"`，二刷 `kind: "secondpass"`；统一字段 **`fileLabel`**（相对路径 `/`）、**`folderTag`**（第一层目录名）、**`subject`**（`- 科目：`）、**`id`**（稳定唯一）；新增字段须同步 **随机抽题 / 筛选 / `appendQuizLog`**。
5. **localStorage 键**：集中在 **`src/storageKeys.js`** 导出常量；写入优先 **`trySetLocalStorage`**、读取 **`tryGetLocalStorage`** / **`readStoredNumber`**。**新增键必须 `smr-` 开头**，并在本文件或 README 中写一句用途。

## 刷题 / 二刷业务规则（勿擅自破坏）

- **文件内刷题**：勾选时题库 **仅** 当前所选二刷 `.md` 中的 `## 题目`，**不含错题本**；与「文件夹 + 科目 + 来源」模式互斥 UI。
- **随机模式**：来源（错题本/二刷）与 **文件夹**（OR）、**科目**（OR）与文件夹条件为 **AND**；科目匹配含 **别名**（如「高数」↔「高等数学」）见 `quizItemMatchesSubjects`。
- **路径**：比较 `fileLabel` 时统一 **`replaceAll("\\", "/")`**。
- **日志**：`appendQuizLog` 结构变更时须考虑旧数据 JSON 解析兼容性（可选字段、勿改键名语义）。

## 样式 `App.css`

- 刷题弹窗类名统一 **`quiz-` 前缀**（如 `.quiz-overlay`、`.quiz-tag`），避免与 `.markdown` / `.viewer` 全局冲突。
- 布局沿用现有 **flex + `min-height: 0`** 思路，勿在无需求时改 `.layout` / `.viewer-split` 根行为。

## Electron 与预加载

- **IPC 暴露面**：仅在 `preload.cjs` 中 `contextBridge.exposeInMainWorld` 列出白名单 API；主进程 `main.cjs` 与预加载字段名保持一致。
- 新增原生能力：**同时** 更新 preload、main、以及 `App.jsx` 中的能力检测与降级文案。

## 交付前检查（每次改软件必做）

1. 在 **`md-reader-app`** 目录执行 **`npm run build`**，确保 Vite 构建通过。
2. 若有 **用户可见行为** 变化，更新根目录 **`README.md`** 对应小节（功能列表、快捷键、刷题说明等）。
3. 可执行 **`npm run lint`**；若 **`react-hooks/set-state-in-effect`** 等对 `App.jsx` 内既有 `useEffect` 报错，属历史写法，**新代码优先避免在 effect 首行同步 setState**；大改 hooks 前需单独评审。

## 禁止项（除非用户明确要求）

- 为「整洁」**大范围重排** `App.jsx` 或无关重命名（难 review、易引入回归）。
- 在错题/二刷正文解析中 **放宽正则** 导致把非题目块吃进题库。
- 引入 **未说明用途** 的新依赖（体积与打包成本）。

## 与仓库根规则的关系

工作区若另有 **`.cursor/rules/*.mdc`**（如全局时间、学习笔记规范），本文件仅约束 **md-reader-app 软件实现**；二者冲突时，**以本文件为软件实现准**。
